version: '3'

services:
    db:
        image: mongo
        container_name: mongodb
        volumes:
            - database:/home
        restart: always
        networks:
            - network1
    auth:
        image: joana/auth:latest
        build: ./auth
        container_name: auth
        restart: always
        ports:
            - '4000:4000'
        networks:
            - network1
    http:
        image: joana/http:latest
        build: ./http
        container_name: http
        ports:
            - '4004:4004'
        networks:
            - network1
            - network2

volumes:
    database:
networks:
    network1:
    network2:

        